import { ArrowUpRight, Database, Trash2 } from "lucide-react";
import { Link } from "react-router-dom";
import { StatusBadge } from "@/components/common/StatusBadge";
import type { Dataset } from "@/types/insightflow";
export function DatasetTable({ datasets, onDelete }: { datasets: Dataset[]; onDelete: (id: string) => void }) { return <div className="overflow-hidden rounded-xl border border-white/[.08] bg-[#101827]" data-testid="dataset-table"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b border-white/[.06] text-[10px] uppercase tracking-[.15em] text-slate-600"><tr><th className="px-5 py-4 font-semibold">Dataset</th><th className="px-5 py-4 font-semibold">Rows</th><th className="px-5 py-4 font-semibold">Columns</th><th className="px-5 py-4 font-semibold">Uploaded</th><th className="px-5 py-4 font-semibold">Status</th><th className="px-5 py-4" /></tr></thead><tbody className="divide-y divide-white/[.05]">{datasets.map((dataset) => <tr key={dataset.id} className="group transition-colors hover:bg-white/[.02]"><td className="whitespace-nowrap px-5 py-4"><div className="flex items-center gap-3"><div className="flex size-8 items-center justify-center rounded-lg bg-emerald-500/10 text-emerald-300"><Database className="size-4" /></div><div><div className="max-w-[260px] truncate text-sm font-medium text-slate-200">{dataset.name}</div><div className="mt-0.5 text-xs text-slate-600">{dataset.size}</div></div></div></td><td className="whitespace-nowrap px-5 py-4 font-mono text-xs text-slate-400">{dataset.rows.toLocaleString()}</td><td className="whitespace-nowrap px-5 py-4 font-mono text-xs text-slate-400">{dataset.columns}</td><td className="whitespace-nowrap px-5 py-4 text-xs text-slate-500">{dataset.uploadedAt}</td><td className="whitespace-nowrap px-5 py-4"><StatusBadge status={dataset.status} /></td><td className="whitespace-nowrap px-5 py-4"><div className="flex justify-end gap-2"><Link to={`/app/datasets/${dataset.id}`} className="flex items-center gap-1 rounded-md px-2 py-1.5 text-xs font-semibold text-blue-300 hover:bg-blue-500/10" data-testid="dataset-analyze-button">Analyze <ArrowUpRight className="size-3" /></Link><button onClick={() => onDelete(dataset.id)} className="rounded-md p-1.5 text-slate-600 hover:bg-rose-500/10 hover:text-rose-300" data-testid="dataset-delete-button"><Trash2 className="size-3.5" /></button></div></td></tr>)}</tbody></table></div></div>; }
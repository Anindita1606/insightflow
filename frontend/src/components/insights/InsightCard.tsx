import { ArrowUpRight, Sparkles } from "lucide-react";
import { StatusBadge } from "@/components/common/StatusBadge";
import type { Insight } from "@/types/insightflow";

export function InsightCard({ insight, compact = false }: { insight: Insight; compact?: boolean }) { return <article className={`rounded-xl border border-white/[.08] bg-[#101827] p-5 transition-colors hover:border-blue-500/25 ${compact ? "" : "space-y-4"}`} data-testid="insight-card-item"><div className="flex items-start justify-between gap-3"><div className="flex items-center gap-2"><span className="flex size-7 items-center justify-center rounded-lg bg-blue-500/10 text-blue-300"><Sparkles className="size-3.5" /></span><span className="text-[10px] font-bold uppercase tracking-[.16em] text-slate-500">{insight.category}</span></div><StatusBadge status={insight.severity} /></div><div><h3 className="font-heading text-sm font-semibold leading-snug text-slate-100">{insight.title}</h3><p className="mt-1 text-xs text-slate-600">{insight.timestamp}</p></div>{!compact && <div className="space-y-3 border-t border-white/[.06] pt-4"><div><div className="mb-1 text-[10px] font-bold uppercase tracking-[.16em] text-slate-600">Finding</div><p className="text-sm leading-relaxed text-slate-300">{insight.finding}</p></div><div><div className="mb-1 text-[10px] font-bold uppercase tracking-[.16em] text-slate-600">Evidence</div><p className="text-xs leading-relaxed text-slate-500">{insight.evidence}</p></div><div className="flex flex-wrap items-center justify-between gap-3 rounded-lg bg-white/[.025] p-3"><div><div className="text-[10px] uppercase tracking-[.14em] text-slate-600">Business impact</div><div className="mt-1 font-mono text-sm text-amber-300">{insight.impact}</div></div><button className="flex items-center gap-1 text-xs font-semibold text-blue-300 hover:text-blue-200" data-testid="insight-action-button">View action <ArrowUpRight className="size-3.5" /></button></div></div>}</article>; }
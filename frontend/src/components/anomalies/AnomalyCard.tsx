import { Check, Crosshair, MapPin } from "lucide-react";
import { StatusBadge } from "@/components/common/StatusBadge";
import type { Anomaly } from "@/types/insightflow";
export function AnomalyCard({ anomaly, onResolve }: { anomaly: Anomaly; onResolve: () => void }) { return <article className="rounded-xl border border-white/[.08] bg-[#101827] p-5 transition-colors hover:border-white/[.15]" data-testid="anomaly-card-item"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div className="flex items-start gap-3"><div className={`mt-0.5 flex size-9 items-center justify-center rounded-lg ${anomaly.severity === "high" ? "bg-rose-500/10 text-rose-300" : "bg-amber-500/10 text-amber-300"}`}><Crosshair className="size-4" /></div><div><div className="flex flex-wrap items-center gap-2"><h3 className="font-heading text-sm font-semibold text-slate-100">{anomaly.metric}</h3><StatusBadge status={anomaly.severity} /></div><div className="mt-1 flex items-center gap-1 text-xs text-slate-600"><MapPin className="size-3" />{anomaly.affected} · {anomaly.detectedAt}</div></div></div>{anomaly.status === "active" ? <button onClick={onResolve} className="flex items-center justify-center gap-1.5 rounded-lg border border-white/[.1] px-3 py-2 text-xs font-semibold text-slate-400 transition-colors hover:border-emerald-500/30 hover:bg-emerald-500/10 hover:text-emerald-300" data-testid="anomaly-resolve-button"><Check className="size-3.5" />Mark resolved</button> : <span className="text-xs text-emerald-400">Resolved</span>}</div><div className="mt-5 grid grid-cols-3 gap-3 border-t border-white/[.06] pt-4"><div><div className="text-[10px] uppercase tracking-[.14em] text-slate-600">Actual</div><div className="mt-1 font-mono text-sm text-white">{anomaly.actual}</div></div><div><div className="text-[10px] uppercase tracking-[.14em] text-slate-600">Expected</div><div className="mt-1 font-mono text-sm text-slate-400">{anomaly.expected}</div></div><div><div className="text-[10px] uppercase tracking-[.14em] text-slate-600">Deviation</div><div className={`mt-1 font-mono text-sm ${anomaly.deviation < 0 ? "text-rose-300" : "text-amber-300"}`}>{anomaly.deviation > 0 ? "+" : ""}{anomaly.deviation}%</div></div></div></article>; }
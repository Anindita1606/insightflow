import { ArrowRight, Check, Target } from "lucide-react";
import { StatusBadge } from "@/components/common/StatusBadge";
import type { Recommendation } from "@/types/insightflow";
export function RecommendationCard({ recommendation, onReview }: { recommendation: Recommendation; onReview: () => void }) { return <article className="rounded-xl border border-white/[.08] bg-[#101827] p-5 transition-colors hover:border-blue-500/25" data-testid="recommendation-card-item"><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-2"><StatusBadge status={recommendation.priority} label={`${recommendation.priority} priority`} /><span className="text-[10px] uppercase tracking-[.14em] text-slate-600">{recommendation.status === "reviewed" ? "Reviewed" : "P1 action"}</span></div><Target className="size-4 text-blue-400" /></div><h3 className="mt-4 font-heading text-base font-semibold leading-snug text-white">{recommendation.title}</h3><p className="mt-2 text-sm leading-relaxed text-slate-500">{recommendation.reason}</p><div className="mt-4 grid grid-cols-2 gap-3 rounded-lg bg-white/[.025] p-3"><div><div className="text-[10px] uppercase tracking-[.14em] text-slate-600">Expected impact</div><div className="mt-1 text-xs font-semibold text-emerald-300">{recommendation.impact}</div></div><div><div className="text-[10px] uppercase tracking-[.14em] text-slate-600">Related metric</div><div className="mt-1 text-xs text-slate-300">{recommendation.metric}</div></div></div><div className="mt-5 flex items-center gap-3"><button className="flex flex-1 items-center justify-center gap-1.5 rounded-lg bg-blue-600 px-3 py-2.5 text-xs font-semibold text-white transition-colors hover:bg-blue-500" data-testid="recommendation-view-button">View analysis <ArrowRight className="size-3.5" /></button><button onClick={onReview} disabled={recommendation.status === "reviewed"} className="flex items-center justify-center gap-1.5 rounded-lg border border-white/[.1] px-3 py-2.5 text-xs font-semibold text-slate-400 transition-colors hover:border-white/20 hover:text-white disabled:cursor-default disabled:text-emerald-300" data-testid="recommendation-review-button">{recommendation.status === "reviewed" ? <Check className="size-3.5" /> : null}{recommendation.status === "reviewed" ? "Reviewed" : "Mark reviewed"}</button></div></article>; }
import { ArrowDownRight, ArrowUpRight } from "lucide-react";
import type { Kpi } from "@/types/insightflow";

export function KpiCard({ kpi }: { kpi: Kpi }) { const positive = kpi.delta >= 0; const accents = { blue: "#3b82f6", emerald: "#10b981", amber: "#f59e0b", violet: "#8b5cf6", cyan: "#06b6d4" }; return <div className="group relative overflow-hidden rounded-xl border border-white/[.08] bg-[#101827] p-5 transition-colors duration-200 hover:border-white/[.15]" data-testid={`kpi-${kpi.id}-card`}><div className="absolute right-0 top-0 h-24 w-24 rounded-full opacity-[.07] blur-2xl" style={{ backgroundColor: accents[kpi.accent] }} /><div className="relative"><div className="flex items-start justify-between"><span className="text-xs font-medium text-slate-500">{kpi.label}</span><span className="rounded-md px-1.5 py-0.5 text-[10px] font-bold" style={{ color: accents[kpi.accent], backgroundColor: `${accents[kpi.accent]}18` }}>LIVE</span></div><div className="mt-3 font-mono text-2xl font-semibold tabular-nums tracking-tight text-white" data-testid={`kpi-${kpi.id}-value`}>{kpi.value}</div><div className={`mt-2 flex items-center gap-1 text-xs font-semibold ${positive ? "text-emerald-400" : "text-rose-400"}`}>{positive ? <ArrowUpRight className="size-3.5" /> : <ArrowDownRight className="size-3.5" />}{Math.abs(kpi.delta)}% <span className="font-normal text-slate-600">vs previous</span></div><div className="mt-1 text-[10px] text-slate-600">{kpi.previous}</div><div className="mt-4 flex h-7 items-end gap-1 opacity-70">{kpi.trend.map((value, index) => <div key={index} className="flex-1 rounded-sm" style={{ height: `${Math.max(20, value)}%`, backgroundColor: accents[kpi.accent] }} />)}</div></div></div>; }
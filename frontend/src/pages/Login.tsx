import { useState } from "react";
import type { FormEvent } from "react";
import { ArrowLeft, ArrowRight, Eye, EyeOff, LockKeyhole, Mail, Sparkles } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import { Logo } from "@/components/brand/Logo";
import { login, register } from "@/services/authService";
import { Button } from "@/components/ui/button";

export default function Login() { const navigate = useNavigate(); const [mode, setMode] = useState<"login" | "signup">("login"); const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [showPassword, setShowPassword] = useState(false); const [loading, setLoading] = useState(false); const [error, setError] = useState(""); const submit = async (event: FormEvent) => { event.preventDefault(); if (!email.trim() || !password.trim() || (mode === "signup" && !name.trim())) { setError("Enter the required details to continue."); return; } setLoading(true); setError(""); try { if (mode === "login") await login(email, password); else await register(name, email, password); navigate("/app"); } catch { setError("We couldn’t start your demo session. Try again."); } finally { setLoading(false); } }; return <div className="grid min-h-svh bg-[#090d16] lg:grid-cols-[.9fr_1.1fr]" data-testid="login-page"><div className="relative hidden overflow-hidden border-r border-white/[.07] lg:block"><div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_20%,rgba(37,99,235,.24),transparent_40%),radial-gradient(circle_at_80%_80%,rgba(16,185,129,.12),transparent_38%)]" /><div className="relative flex h-full flex-col justify-between p-12"><Logo /><div className="max-w-md"><div className="mb-5 flex items-center gap-2 text-[10px] font-bold uppercase tracking-[.2em] text-blue-400"><Sparkles className="size-3.5" />Decision intelligence for modern teams</div><h1 className="font-heading text-4xl font-semibold leading-tight tracking-[-.04em] text-white">Know what changed. <span className="text-slate-500">Know what to do next.</span></h1><p className="mt-5 text-sm leading-relaxed text-slate-500">A calmer way to navigate business performance, with every insight connected to evidence and action.</p><div className="mt-9 grid grid-cols-2 gap-3"><div className="rounded-xl border border-white/[.08] bg-white/[.03] p-4"><div className="font-mono text-2xl text-white">94.8</div><div className="mt-1 text-[10px] text-slate-600">Signal clarity index</div></div><div className="rounded-xl border border-white/[.08] bg-white/[.03] p-4"><div className="font-mono text-2xl text-emerald-300">+12.4%</div><div className="mt-1 text-[10px] text-slate-600">Revenue growth found</div></div></div></div><div className="text-[10px] uppercase tracking-[.16em] text-slate-700">InsightFlow · Private demo workspace</div></div></div><div className="flex items-center justify-center px-5 py-10 sm:px-10"><div className="w-full max-w-md"><Link to="/" className="mb-12 inline-flex items-center gap-2 text-xs text-slate-500 hover:text-white lg:hidden"><ArrowLeft className="size-3.5" />Back to overview</Link><div className="mb-8 lg:hidden"><Logo /></div><div className="mb-8"><div className="mb-3 text-[10px] font-bold uppercase tracking-[.2em] text-blue-400">{mode === "login" ? "Welcome back" : "Start your workspace"}</div><h2 className="font-heading text-3xl font-semibold tracking-tight text-white">{mode === "login" ? "Sign in to InsightFlow" : "Create your demo account"}</h2><p className="mt-2 text-sm text-slate-500">{mode === "login" ? "Use any non-empty credentials to explore the product." : "Set up a profile for your private product tour."}</p></div><div className="mb-6 flex rounded-lg border border-white/[.08] bg-white/[.025] p-1"><button onClick={() => { setMode("login"); setError(""); }} className={`flex-1 rounded-md py-2 text-xs font-semibold ${mode === "login" ? "bg-blue-500/15 text-blue-300" : "text-slate-500"}`} data-testid="login-tab-button">Log in</button><button onClick={() => { setMode("signup"); setError(""); }} className={`flex-1 rounded-md py-2 text-xs font-semibold ${mode === "signup" ? "bg-blue-500/15 text-blue-300" : "text-slate-500"}`} data-testid="signup-tab-button">Sign up</button></div><form onSubmit={submit} className="space-y-4" data-testid="auth-form">{mode === "signup" && <label className="block text-xs font-medium text-slate-400">Full name<div className="relative mt-2"><input value={name} onChange={(event) => setName(event.target.value)} placeholder="Maya Chen" className="h-11 w-full rounded-lg border border-white/[.1] bg-white/[.03] px-3 text-sm text-white outline-none placeholder:text-slate-700 focus:border-blue-500/60" data-testid="login-name-input" /></div></label>}<label className="block text-xs font-medium text-slate-400">Work email<div className="relative mt-2"><Mail className="absolute left-3 top-3.5 size-4 text-slate-600" /><input value={email} onChange={(event) => setEmail(event.target.value)} type="email" placeholder="you@company.com" className="h-11 w-full rounded-lg border border-white/[.1] bg-white/[.03] pl-10 pr-3 text-sm text-white outline-none placeholder:text-slate-700 focus:border-blue-500/60" data-testid="login-email-input" /></div></label><label className="block text-xs font-medium text-slate-400">Password<div className="relative mt-2"><LockKeyhole className="absolute left-3 top-3.5 size-4 text-slate-600" /><input value={password} onChange={(event) => setPassword(event.target.value)} type={showPassword ? "text" : "password"} placeholder="••••••••" className="h-11 w-full rounded-lg border border-white/[.1] bg-white/[.03] pl-10 pr-10 text-sm text-white outline-none placeholder:text-slate-700 focus:border-blue-500/60" data-testid="login-password-input" /><button type="button" onClick={() => setShowPassword(!showPassword)} className="absolute right-3 top-3 text-slate-600 hover:text-slate-300" data-testid="password-visibility-button">{showPassword ? <EyeOff className="size-4" /> : <Eye className="size-4" />}</button></div></label>{error && <p className="text-xs text-rose-300" data-testid="auth-error-message">{error}</p>}<Button type="submit" disabled={loading} className="h-11 w-full bg-blue-600 text-white hover:bg-blue-500" data-testid="login-submit-button">{loading ? "Opening workspace…" : mode === "login" ? "Enter workspace" : "Create workspace"}<ArrowRight className="ml-2 size-4" /></Button></form><p className="mt-7 text-center text-[11px] leading-relaxed text-slate-600">By continuing, you agree to the InsightFlow demo terms. No data is stored outside this browser session.</p></div></div></div>; }